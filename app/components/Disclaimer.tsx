const Disclaimer = () => (
  <header>
    <div>
      <p>Hi, this is a typescript tool.</p>
      <p>
        Paste your JSON in the input field and types are generated bellow. In
        order to avoid error for now, use a JSON format as in the example with
        double quotes in each key.
      </p>
    </div>
  </header>
);

export default Disclaimer;
